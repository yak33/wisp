# Wisp 开发进度

> 记录里程碑状态与关键技术决策，按时间倒序更新。

## 里程碑总览

| 里程碑 | 内容 | 状态 | 日期 |
|--------|------|------|------|
| M0 | 技术选型与 PoC 验证 | ✅ 完成 | 2026/08/15 |
| M1 | 剪贴板历史（文本） | ✅ 完成 | 2026/08/15 |
| M2 | 回车直达粘贴（焦点还原 + SendInput） | ✅ 完成 | 2026/08/15 |
| M4 | 备忘快贴（标签化片段库） | ✅ 完成 | 2026/08/16 |
| M4.5 | 主页导航（uTools 风格图标网格）+ 窗口拖动 | ✅ 完成 | 2026/08/16 |
| M3 | 剪贴板图像 / 文件支持 | 📋 计划 | — |
| M5 | IP 工具（三段 IP + 延迟面板） | 📋 计划 | — |
| M6 | 开机自启 + release 打包分发 | 📋 计划 | — |

---

## 补丁 · 失焦隐藏改钉子图标 — 2026/08/16

头部的「失焦自动隐藏」复选框换成钉子图标按钮（钉住 = 失焦不隐藏，
划掉 = 失焦自动隐藏），语义对齐主流应用的置顶钉，标题栏更轻。

- [x] gpui-component 无 pin 图标：自有 SVG 经 `include_bytes!` 内嵌，
      `WispAssets` 实现 `AssetSource`（先查自有、回落 gpui-component-assets），
      `WispIcon` 实现 `IconNamed` 借三层 blanket impl 直通 `Button::icon`，零新依赖
- [x] 状态与提示随 `auto_hide` 联动，悬停说明当前行为与点击效果
- [x] 按钮保留拖动区内的 `.occlude()` 包裹（否则点击被 HTCAPTION 吞掉）

---

## 补丁 · 列表悬停预览 — 2026/08/16

长内容在行内只有单行摘要，鼠标悬停 300ms 弹出预览：保留换行截断 500 字符，
尾注交代总字符数（剪贴板）/ 备注·标签·总字符数（备忘）。截断预览在行渲染时
`chars().take(500)` 惰性求值，超大条目（上限 2MB）不产生额外开销；完整内容
查看留给后续的详情层。

- [x] gpui 原生 `StatefulInteractiveElement::tooltip` + `tooltip_show_delay`，
  内容经 gpui-component `Tooltip::element` 获得统一样式（popover 底/边框/阴影）
- [x] 换行用逐行子元素保留——此版 gpui 的 `WhiteSpace` 只有 Normal/Nowrap，
  无 pre-wrap；空行以空格占位维持行高
- [x] 两视图各抽 `render_item`（行 + 悬停 + 点击），虚拟列表闭包回归清爽

---

## 补丁 · 记住上次页面 + 剪贴板分类 — 2026/08/16

M4.5 的两处修正：页面记忆跨重启生效；图像/文件从主页网格撤下——
它们是剪贴板历史的内部分类，不是独立功能。

- [x] 上次页面持久化：新增 `config.rs`（key=value 手写解析，零依赖），`open_page` 随写随存到 `wisp.cfg`，启动恢复
- [x] core 新增 `ClipFilter`（全部 / 文本 / 图像 / 文件 / 收藏），store 查询改动态 SQL，分类与关键字可叠加
- [x] 剪贴板视图加分类标签行：点击或 `Alt+1~5` 切换；图像 / 文件显示 M3 占位提示
- [x] 主页移除"图像与文件"卡片；新增表驱动测试 1 例（8 例全绿）
- [x] **拖动修复（三连坑，最终走原生机制）**：
      1. `SendMessageW(WM_NCLBUTTONDOWN, HTCAPTION)` 在事件处理器内同步进模态移动循环，
         借用未释放 + 循环内消息重入 → `RefCell already borrowed` panic。
      2. 改 Post 后 NC 消息回流成合成鼠标事件进 GPUI 输入分发，被视图树消费，
         到不了 `DefWindowProc`（不崩也不动）；Post `WM_SYSCOMMAND/SC_MOVE` 同样无效。
      3. **正解**：头部区域标记 `.window_control_area(WindowControlArea::Drag)`——
         GPUI Windows 原生支持：`WM_NCHITTEST` 对该区域返回 `HTCAPTION`，Windows 自己
         接管移动，零消息投递、零重入风险；此前"边框可缩放"正是同一命中测试的另一半（HTTOP 等）。
      4. **后续坑**：命中链默认穿透 Normal 行为的子元素 hitbox，拖动区的按钮/复选框点击
         全被当成标题栏拖拽吞掉。可点击子元素必须 `.occlude()`（BlockMouse）截断命中链
         （Zed 标题栏的 application_menu / collab 按钮同样如此处理）。

---

## M4.5 · 主页导航 + 窗口拖动 — 2026/08/16

把 TabBar 换成 uTools 风格的主页：唤起先落功能网格，点进图标再进功能页。
新功能此后只需在主页表里加一行，导航结构零改动。

- [x] 新增 `HomeView`：搜索框过滤功能（中文名 / 英文别名），↑↓ 选择、回车 / 单击进入
- [x] 功能表驱动：剪贴板 / 备忘快贴可用；图像与文件、IP 工具置灰占位（规划中）
- [x] Esc 语义改为逐层外退：编辑态取消编辑（消费不外冒）→ 功能页回主页 → 主页隐藏窗口
- [x] Ctrl+1 / Ctrl+2 任意页直达；功能页头部加「‹ 主页」返回
- [x] 窗口拖动：标题区按下左键 → `ReleaseCapture` + `SendMessageW(WM_NCLBUTTONDOWN, HTCAPTION)`，交系统模态移动循环，无需自追踪鼠标
- [x] `cargo build` + `cargo test -p wisp-core`（7 例）全绿

**踩坑**：Rust 2024 的 RPIT 捕获全部入参生命周期——卡片渲染函数返回 `impl IntoElement` 会借用
`&self`/`cx`，导致卡片无法逃出 `map` 闭包；改返回具体类型 `Stateful<Div>` 解决（与 M1 第 3 条同源）。

---

## M4 · 备忘快贴 — 2026/08/16

标签化的文本片段库。粘贴链路直接复用 M2，因此这一版的工作量集中在数据模型与视图层次。

**core**

- [x] 片段 / 标签 / 关联三张表，标签多对多，外键级联删除
- [x] 标签串用 `group_concat` 在 SQL 内聚合，避免逐条回查的 N+1
- [x] 保存与删除走事务，并自动清理不再被引用的孤立标签
- [x] 筛选维度：全部 / 无标签 / 指定标签；关键字同时匹配内容与备注
- [x] 标签解析：中英文逗号、顿号、空白均可分隔，去重且保序
- [x] 表驱动测试 4 例（标签筛选、无标签筛选、备注检索、孤立标签清理）

**app**

- [x] 新增根视图 `WispView`，标签页在剪贴板与备忘之间切换（Ctrl+1 / Ctrl+2）
- [x] 列表态 / 编辑态双形态共用一个视图；编辑态 Ctrl+S 保存、Esc 取消
- [x] 左侧标签侧栏带计数；在某标签下新建时自动预填该标签
- [x] ↑↓ 选择、回车直达粘贴、Ctrl+N 新建、Ctrl+E 编辑

**重构**：窗口级关注点（唤起聚焦、失焦隐藏、标签切换）从 `ClipboardView` 上移到 `WispView` 统一收口——两个子视图并存后，各自监听窗口激活会互相抢焦点。

---

## M2 · 回车直达粘贴 — 2026/08/15

选中条目后直接落到用户原来所在的输入框，不再需要手动 Ctrl+V。这条链路同时是备忘快贴（M4）的地基。

- [x] 唤起前记录前台窗口（`GetForegroundWindow`），并排除 Wisp 自身
- [x] 交付链路：写剪贴板 → 隐藏窗口 → `SetForegroundWindow` 还原焦点 → 模拟 Ctrl+V
- [x] 粘贴前抬起残留的物理修饰键（Alt/Shift/Win），避免 `Ctrl+V` 被污染成组合键
- [x] 全程在独立线程执行（80ms 焦点落定 + 20ms 击键间隔），UI 线程零阻塞
- [x] 目标窗口失效时静默降级为"仅复制"，内容仍在剪贴板
- [x] `Enter` 直达粘贴，`Ctrl+Enter` 仅回填剪贴板

**设计要点**：记录时机是全链路的关键——必须在 Wisp 抢到前台**之前**捕获，否则记到的是自己。

---

## M1 · 剪贴板历史（文本）— 2026/08/15

首个可用版本，从零到常驻运行。

**core 层（wisp-core）**

- [x] Win32 message-only 窗口 + `WM_CLIPBOARDUPDATE` 事件驱动监听（零轮询，后台 CPU 0%）
- [x] SQLite 存储：WAL 模式、FNV-1a 指纹去重（哈希命中后仍比对原文，碰撞零误判）
- [x] 重复内容置顶而非重复入库（含自我复制回环，即"从 Wisp 复制"也会置顶该条）
- [x] 模糊检索：LIKE 通配符按字面转义；置顶项恒排最前；上限 500 条
- [x] 超大文本熔断（>2MB 不入库）；剪贴板占用时短退避重试读取
- [x] 表驱动测试 3 例全绿（去重置顶 / 通配符转义 / 置顶排序）

**app 层（wisp-app）**

- [x] 无边框客户端装饰窗口，预创建 + `ShowWindow` 显隐（唤起体感零延迟）
- [x] 全局快捷键降级链：`Alt+Space` → `Ctrl+Alt+Space` → ``Alt+` ``（实际生效键显示于状态栏与托盘）
- [x] 托盘：双击唤起、右键菜单退出
- [x] 唤起即聚焦搜索框；窗口激活自动补查（隐藏期间错过的变更不丢）
- [x] ↑↓ 键盘导航 + 回车/点击复制并隐藏 + Ctrl+P 置顶 + Esc/失焦隐藏
- [x] 虚拟列表渲染，数据量与帧率无关

**实测**：常驻内存 ~27MB（debug 构建）。

**踩坑记录**

1. windows crate 0.58 与 0.61 的参数风格不同（`Param<T>` vs `Option<T>`），统一升至 0.61；
2. `RegisterClassW` 被 `Win32_Graphics_Gdi` feature 门控（WNDCLASSW 含 GDI 类型），workspace 构建时被依赖树的 feature 联合掩盖，单测 `-p wisp-core` 才暴露——workspace 显式补齐该 feature；
3. Rust 2024 的 RPIT 默认捕获入参生命周期，返回元素的辅助函数需返回具体类型（如 `Div`）而非 `impl IntoElement`。

---

## M0 · 技术选型与 PoC — 2026/08/15

**结论：GPUI + gpui-component 路线成立。**

| 验证项 | 结果 |
|--------|------|
| gpui + gpui-component 在 Windows 11 / Rust 1.97 构建 | ✅ 一次通过 |
| 中文 IME（候选框跟随、上屏） | ✅ 用户实测通过 |
| 5 万条虚拟列表滚动 + 即时过滤 | ✅ 流畅（用户评价"比 uTools 还丝滑"） |
| 托盘 + 全局快捷键与 gpui 事件循环共存 | ✅ 通过 |
| 窗口隐藏 / 唤醒 / 失焦隐藏 / 唤起即聚焦 | ✅ 通过 |
| 无边框窗口客户端装饰（圆角 / 边框） | ✅ 通过 |
| PoC 常驻内存（含 5 万条 mock 数据，debug） | 47MB |

**关键决策与依据**

- **不选 Tauri**：本场景性能体感两者无差（虚拟列表 / 窗口显隐均不卡在 UI 框架），选 GPUI 的真实动机是原生单进程 + 学习价值；gpui-component（60+ 组件，LongPort 生产验证）把组件成本降到可接受。
- **core 与壳解耦**：`wisp-core` 零 UI 依赖，选型风险被隔离在壳层，未来可随时换壳。
- **gpui 依赖策略**：gpui-component 锁 rev（`be6f5a7`），gpui 跟随其要求从 Zed 主干解析，由 `Cargo.lock` 固定。

**遗留技术债**

- [ ] 事件泵为 50ms 轮询（托盘/快捷键/变更信号），待换 `set_event_handler` + 主线程投递
- [ ] 进程退出时托盘图标偶发残留至鼠标划过（Windows 常见现象，低优先级）
